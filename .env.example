# ==========================================
# OpenAI / LLM 模型設定
# ==========================================
# 你的 OpenAI API Key (必填)
OPENAI_API_KEY=sk-...

# 使用的模型名稱 (例如: gpt-4o, gpt-4o-mini, gpt-3.5-turbo)
OPENAI_COMPLETION_MODEL=gpt-4o-mini

# OpenAI API 的基礎網址 (若使用 Azure 或其他兼容服務可修改)
OPENAI_BASE_URL=https://api.openai.com


# ==========================================
# Line Bot 設定
# ==========================================
# Line Developers Console 中的 Channel Access Token (必填)
LINE_CHANNEL_ACCESS_TOKEN=...

# Line Developers Console 中的 Channel Secret (必填)
LINE_CHANNEL_SECRET=...

# 用戶觸發機器人的指令前綴
BOT_TRIGGER_WORD=/bot

# 避免連續發送訊息過快導致被 Line 封鎖的間隔 (秒)
LINE_MIN_PUSH_INTERVAL_SEC=1.2

# 每個對話群組保留的歷史訊息數量 (用於上下文理解)
MAX_HISTORY_MESSAGES=10

# 單則訊息的最大長度限制 (Line 上限約 5000 字)
MAX_MESSAGE_LENGTH=4800


# ==========================================
# 新聞抓取設定
# ==========================================
# 只抓取最近幾天內的新聞
NEWS_FETCH_DAYS_LIMIT=3

# 每次抓取目標的新聞數量
NEWS_FETCH_TARGET_COUNT=6

# 是否使用無頭模式執行 Selenium (True=不顯示瀏覽器視窗, False=顯示)
# 在伺服器環境通常設為 true，本地除錯可設為 false
SELENIUM_HEADLESS=true

# 視覺分隔延遲 (秒)，用於控制訊息發送節奏
VISUAL_SEPARATION_DELAY=1.0


# ==========================================
# 功能開關與行為設定
# ==========================================
# 是否允許使用模型的 Reasoning Content (若模型支援) 作為備援
ALLOW_REASONING_FALLBACK=false

# 是否在回覆中顯示 LLM 的思考過程 (<think> 標籤內容)
SHOW_THINKING_PROCESS=false

# 當 LLM 回覆為空時，是否嘗試使用原始回應作為備援
FALLBACK_ON_EMPTY=true

# 程式啟動時是否立即執行一次新聞推播任務 (方便測試用)
RUN_JOB_ON_STARTUP=False


# ==========================================
# 測試與除錯
# ==========================================
# 指定一個 Line User ID 作為測試目標 (啟動時會強制加入推播名單)
TARGET_USER_ID_FOR_TESTING=

# Flask Web Server 運行的 Port
PORT=5000
